 {% set backgroundUrl = "background-image:url(" ~ file_url(node.field_image.entity.field_media_image.entity.fileuri) ~ ")" %}
{% set imgFile  =  file_url(node.field_image.entity.field_media_image.entity.fileuri) %}

{% if imgFile %}
    <section id="hero"  style={{ backgroundUrl }}  class="hero page-hero d-flex align-items-center mt-6">
{% else %}
    <section>
{% endif %}
  <div class="container">
    <div class="row">
    {% if imgFile %}
      <div class="col-lg-5 col-xl-6 page-content">
    {% else %}
      <div class="col-12 page-content">
    {% endif %}
          <h1>{{ label }}</h1>          
          {{ content.body }}
      </div>
    </div>
  </div>
</section> 

